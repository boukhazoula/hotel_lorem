{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1 class="titre-index">Category</h1>
    <button class="grey-button"><a href="{{ path('app_category_index') }}">back to list</a></button>
    <div class="flex-box">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    
    <div class="flex-box special">
    <button class="warning-button-lg"><a href="{{ path('app_category_edit', {'id': category.id}) }}">edit</a></button>

    {{ include('category/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}
