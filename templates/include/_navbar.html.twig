<nav class="navbar">
	<ul>
		<li class="brand">
			<a class="navbar-brand" href="{{ path('app_home') }}">Hotel Pro++</a>
		</li>
		{% if app.user %}
			<li class="special deconnexion">
				<a class="nav-link" aria-current="page" href="{{ path('app_logout') }}">Deconnexion</a>
			</li>
			{% if is_granted('ROLE_ADMIN') %}
                <li>
					<a class="nav-link" aria-current="page" href="{{ path('app_category_index') }}">Categories <i class="fa-solid fa-screwdriver-wrench"></i></a>
				</li>
                <li>
					<a class="nav-link" aria-current="page" href="{{ path('app_chambre_index') }}">Chambre <i class="fa-solid fa-screwdriver-wrench"></i></a>
				</li>
                 <li>
					<a class="nav-link" aria-current="page" href="{{ path('app_reservation_index') }}">Réservations <i class="fa-solid fa-screwdriver-wrench"></i></a>
				</li>
			{% endif %}
			<li>
				<a class="nav-link" aria-current="page" href="{{ path('app_spa') }}">Notre Spa</a>
			</li>
			<li>
				<a class="nav-link" aria-current="page" href="{{ path('app_resto') }}">Restaurant</a>
			</li>
			
		{% else %}
			<li class="special">
				<a class="nav-link" aria-current="page" href="{{ path('app_register') }}">Inscription</a>
			</li>
			<li class="special">
				<a class="nav-link" aria-current="page" href="{{ path('app_login') }}">Connexion</a>
			</li>
		{% endif %}
	</ul>
</nav>
